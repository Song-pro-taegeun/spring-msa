spring:
  config:
    activate:
      on-profile: "local"
  jpa:
    open-in-view: false
    generate-ddl: false
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
        format_sql: true
    show-sql: false
  # 최초 테넌트 세팅 시 마스터권한 정보
  master-datasource:
    url: "jdbc:mariadb://localhost:3306/msa_order"
    username: "root"
    password: "1234"
    driver-class-name: org.mariadb.jdbc.Driver
  base-datasource:
    url: "jdbc:mariadb://localhost:3306/"
  base-schema-name: msa_order
  # Redis
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      connect-timeout: 2s
      timeout: 2s
  cache:
    type: redis
    redis:
      time-to-live: 10m
      cache-null-values: false
      key-prefix: "order-service:"
      use-key-prefix: true

kafka:
  host-port: "localhost:9092"